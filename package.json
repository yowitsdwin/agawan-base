{
  "name": "agawan-base-multiplayer",
  "version": "1.0.0",
  "description": "Multiplayer Filipino Agawan Base game",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "prepare-docs": "rm -rf docs && mkdir docs && cp -r client/* docs/",
    "deploy-gh": "npm run prepare-docs && git add docs && git commit -m 'Update docs for GitHub Pages' && git push origin main"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": ["game", "multiplayer", "nodejs", "socket.io"],
  "author": "Your Name",
  "license": "MIT"
}
